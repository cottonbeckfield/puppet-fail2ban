# This file is managed by Puppet.

[ssh-iptables]
enabled  = true
filter   = sshd
action   = iptables[name=SSH, port=ssh, protocol=tcp]
           sendmail-whois[name=SSH, dest=<%= teamEmail %>, sender=fail2ban@<%= scope.lookupvar('fqdn')%>]
logpath  = /var/log/secure
