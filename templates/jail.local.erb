# This file is managed by Puppet.

[ssh-iptables]
enabled  = true
filter   = sshd
action   = iptables[name=SSH, port=ssh, protocol=tcp]
           sendmail-whois[name=SSH, dest=<%= t_email %>, sender=fail2ban@<%= scope.lookupvar('fqdn')%>]
logpath  = /var/log/secure
